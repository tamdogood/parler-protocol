.idea
/target
**/*.rs.bk
Cargo.lock.bak

# Python bytecode (the Hermes plugin)
__pycache__/
*.pyc

# Local runtime / secrets
.parler/
*.creds
*.seed

# Demo runtime (seed-demo.sh / hackathon-demo.sh / demo-handoff.sh / dogfood.sh)
/.demo/
/.hackathon-demo/
/.demo-handoff/
/.dogfood/

# Architect-loop runtime state (worktrees, research findings, dispatch blocks)
.architect/

# Scratch (reference clone, downloaded binaries) — also gitignored by the workspace
.context/

# Personal learning notes (not part of the project)
/COTAL-ARCHITECTURE.md

.DS_Store
