.idea
/target
**/*.rs.bk
Cargo.lock.bak

# Python bytecode (the Hermes plugin)
__pycache__/
*.pyc

# Local runtime / secrets
.parler/
*.creds
*.seed

# Demo runtime (seed-demo.sh) + website build artifacts
/.demo/
/web/node_modules/
/web/.next/
/web/out/
/web/next-env.d.ts
/web/*.tsbuildinfo

# Scratch (reference clone, downloaded binaries) — also gitignored by the workspace
.context/

# Personal learning notes (not part of the project)
/COTAL-ARCHITECTURE.md

.DS_Store
